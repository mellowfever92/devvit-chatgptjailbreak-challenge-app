export const challengeKey = (challengeId) => `challenge:${challengeId}`;
export const challengeSlugKey = (slug) => `challenge:slug:${slug}`;
export const challengeListKey = 'challenge:list';
export const submissionsKey = (challengeId) => `challenge:${challengeId}:submissions`;
export const submissionKey = (submissionId) => `submission:${submissionId}`;
export const leaderboardApprovedKey = 'leaderboard:approved';
export const leaderboardVotesKey = 'leaderboard:votes';
export const voteKey = (challengeId, userId) => `challenge:${challengeId}:vote:${userId}`;
export const rateLimitKey = (challengeId, userId) => `ratelimit:${challengeId}:${userId}`;
export const apiStatsKey = 'stats:api-usage';
